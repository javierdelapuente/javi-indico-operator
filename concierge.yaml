juju:
  channel: 2.9/stable
  model-defaults:
    test-mode: "true"
    automatically-retry-hooks: "false"

providers:
  microk8s:
    enable: true
    bootstrap: true
    addons:
      - dns
      - ingress
      - rbac
      - hostpath-storage
